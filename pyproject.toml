[project]
name = "slurm-utils"
version = "1.0.0"
description = "MCP server for SLURM GPU job management"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]",
]

[project.optional-dependencies]
dev = ["pytest"]
